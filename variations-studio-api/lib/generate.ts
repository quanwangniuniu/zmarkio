/**
 * Re-export barrel — preserves backward compatibility.
 * Actual implementation lives in src/domains/generate/.
 */
export { runCustomGenerate, type GenerateBatchResponse } from '@/src/domains/generate';
